export enum Status_Order_ENUM {
    PENDING = 'PENDING',
    DELIVERED = 'DELIVERED'
}

export enum Pay_Payment_ENUM {
    CARD = 'CARD',
    CASH = 'CASH'
}

export enum Status_Payment_ENUM {
    PENDING = 'PENDING',
    PAID = 'PAID'
}